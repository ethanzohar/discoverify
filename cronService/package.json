{
    "name": "discoverify-cronService",
    "version": "1.0.0",
    "scripts": {
        "start": "nodemon app.js",
        "deploy": "git pull && npm install && pm2 reload discoverify-cronService",
        "logs": "pm2 logs discoverify-cronService --lines 500"
    },
    "dependencies": {
        "cron": "^1.8.2",
        "mongoose": "^5.10.13"
    },
    "devDependencies": {
        "nodemon": "^2.0.6"
    }
}